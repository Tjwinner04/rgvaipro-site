# Copy to `.env.local` (do not commit `.env.local`).

# n8n Webhook URL — preferred (server-only).
N8N_CONTACT_WEBHOOK_URL=

# Optional: same URL for dev / client “configured” banner.
NEXT_PUBLIC_N8N_CONTACT_WEBHOOK_URL=
